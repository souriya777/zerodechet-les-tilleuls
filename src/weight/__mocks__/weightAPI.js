import { WEEK } from '../../utils/common-test/common-data'
import { WEEK_DATA } from './weekData'

class WeightAPI {
  getWeightListBtwDates = (uid, beginDate, endDate) => {

    return WEEK_DATA
  }
  
}

export default new WeightAPI()